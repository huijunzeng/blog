spring:
  cloud:
    nacos:
      config: # nacos配置中心地址配置
        server-addr: ${SPRING_CLOUD_NACOS_CONFIG_SERVERADDR:129.211.34.120:8848}
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev} # 根据不同环境切换配置环境